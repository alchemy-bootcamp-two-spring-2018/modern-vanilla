import Template from './Template.js';

const template = new Template(({ title, category }); => {

} )
    return `
    <h4>${title} ${category}</h4>
    
    
    <ul>
        <li>Title:</li>
        <li>Category:</li>

    </ul>
        
        `;

});
