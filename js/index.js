import App from './App.js';

const app = new App();
const root = document.getElementById('root');
root.appendChild(app.render());

//this seems to be doing the job that Main.js did in our other projects